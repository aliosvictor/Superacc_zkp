# Default configuration for training on the full Cora feature set.
data_dir: data/cora
epochs: 200
learning_rate: 0.01
weight_decay: 0.0005
hidden_size: 16
dropout: 0.5
precision: f32
seed: 42
patience: 20
use_gpu: false
